import React from "react";
import OptionWrapper from "../components/common/OptionWrapper";
import GenderAgeOption from "../components/genderAgeOption/GenderAgeOption";

const FirstOption = () => {
  return (
    <OptionWrapper>
      <GenderAgeOption />
    </OptionWrapper>
  );
};

export default FirstOption;
